using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Ucpf.Languages.C
{
	class Memo
	{
		/*
		Action<CType> generation(CModelToCode g)
		{
			Action<CType> action = new Action<CType>(type => g.Generate(type));
			return action;
		}

		int selectFunc(XElement e)
		{
			return 1;
		}

		IEnumerable<TR> Select<T, TR>(IEnumerable<T> source, Func<T, TR> map)
		{
			foreach (var item in source)
			{
				yield return map(item);
			}
		}
		*/
	}
}
